my-config.name : dev

spring :
  datasource :
    uri : myEnvironment1
    username : root
    password : password

management :
  endpoints :
    web:
      exposure :
        include : "*"
  endpoint :
    shutdown :
      enabled : true
